import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "RecoverKit Alternatives & Comparisons",
  description: "Compare RecoverKit to other payment recovery and dunning tools. See why hundreds of SaaS founders choose RecoverKit.",
};

export default function AlternativesLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
